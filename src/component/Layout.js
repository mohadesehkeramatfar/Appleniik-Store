import Filter from "./Header/Filter";
import Header from "./Header/Header";

const Layout = () => {
  return (
    <section className="container  px-2 mx-auto xl:max-w-screen-xl">
      <Header />
 
    </section>
  );
};

export default Layout;
